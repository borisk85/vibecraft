# STATE — Vibecraft (vibecraft.kz)
Обновлено: 2026-09-08

## ПРАВИЛА ЭТОГО ФАЙЛА
- Единственное место рабочих заметок и памяти между сессиями.
- Читаю в начале каждой сессии, обновляю после каждого этапа.
- В работе всегда ровно одна задача.

## В РАБОТЕ (ровно 1)
нет активной — очередь пуста

## ОЧЕРЕДЬ (по порядку)
- пусто. Задачи сюда добавляет только Boris командой «добавь в очередь».

## СЛЕДУЮЩИЙ ШАГ ПОСЛЕ ПЕРЕРЫВА
Шаблон писем закрыт и заморожен, задач по нему нет. Следующую задачу ставит Boris.

Блог закрыт: 12 статей, все три аудита пройдены, очередь тем пуста. Тему ставит Boris.
Ключи и разбор — docs/seo-blog-plan.md. По статье три скила: /audit-article, /review-edits, /audit-live.

Не проверено на живой статье: третий проход бота _polish_structure (добавлен 03.08).
Первая новая статья покажет; если брак прежний — смотреть в логах «Структурный проход применен».

## ИДЕИ (не задачи — в работу только по команде Boris)
- GEO (оптимизация под AI-поиск) и Speed Boost (Core Web Vitals) как услуги в Services.tsx + калькулятор. Цены не подтверждены. Источник: memory/project_state.md, блок «(старое)» от 2026-06-15.
- SEO-статьи под ключи из docs/seo-blog-plan.md (пишет vela-marketing-bot -> PR -> /admin/blog). Источник: тот же блок от 2026-06-15.

## СДЕЛАНО (с датой, новое сверху)
- [2026-09-08] Шаблон писем клиентам доведен и заморожен. Иконки соцсетей в подвале
  отдавались PNG с белой заливкой и в темной теме почты читались белыми квадратами —
  теперь монохром #8a8a8a на прозрачном фоне. Лого в шапке было мелким: квадрат 40px
  с иконкой 28px вырос до 56/40, текст рядом после замера на мобильном ужат до 18px
  (vibecraft) и 10px (подпись). Тестовые письма слал себе на bkomarov85@gmail.com через
  Resend, третье письмо чистое (e915445, 1cdcbe8).
  По жалобе, что Дархану ушло не то: письмо проверено у Resend по id из Telegram —
  клиенту ушел ровно отредактированный текст. Мой промах был в источнике: показал
  письмо из Gmail на почту Boris и назвал его письмом клиенту. Правило записано в
  хук .claude/inject_rules.py (e1d1037).
- [2026-09-08] Ответ Дархану ушел (delivered), по нему разобрана вся почтовая цепочка.
  Черновик в Telegram копировал текст Boris один в один: reply длиннее 160 символов
  считался готовым письмом и мимо Claude уходил в черновик — механика перенесена из
  support-bot (bot.py:551). Порог оставлен как в референсе, но клавиатура приведена к
  нему же: одна кнопка бывает только после «Редактировать», обычный черновик идет с
  двумя (234674f).
  Судьба письма больше не живет в панели Resend: вебхук /api/resend с проверкой подписи
  Svix шлет в чат заявок отбой, жалобу на спам, задержку и открытие письма (a3e532e,
  1d0abab). Вебхук заведен через API Resend, секрет в Vercel, отслеживание открытий у
  домена включено. Домен verified: SPF на send.vibecraft.kz, DKIM resend._domainkey,
  DMARC p=none.
  Вилки бюджета в форме выровнены по прайсу: 1 500 000 сдвинута на 1 200 000 (цена входа
  MVP веб-приложений), нижний пункт стал 150 000 – 500 000 вместо «до 500 000» — дешевле
  ботов услуг нет. Тарифы поддержки в форме сверены с прайсом, расхождений нет (1602851).
  Хук check_no_pause научен ловить свой класс факапа: назвал проблему словами и за ход не
  сделал ни одной правки — ход блокируется (5028c74).
- [2026-09-07] Первая заявка с сайта (Дархан, MVP веб-приложений, 1,5-3 млн ₸). Разобрано,
  почему в ней не было телефона и источника: поля телефона в форме нет, «Как вы о нас узнали»
  необязательное и клиент его пропустил, а сбора utm и реферера в коде не было вовсе.
  По команде Boris форма научена запоминать метки кампании, реферер и страницу входа при
  первом заходе (sessionStorage) и слать их вместе с заявкой; в Telegram добавлены строки
  Кампания, Пришел с, Вошел на, Отправил со страницы. Видимая часть формы не менялась.
  Коммит 31bc778, npm run build прошел, живая отправка на проде не проверялась.

## КОНТЕКСТ ПРОЕКТА (кратко, только факты)
- Стек: Next.js 16.2.4 (App Router) + React 19.2 + TS 5 + Tailwind 4. Supabase, @anthropic-ai/sdk, Resend, Upstash Redis+Ratelimit, @react-pdf/renderer, framer-motion, BlockNote (админ-редактор). Node 24.x, Husky.
- Деплой: Vercel, git push в main -> auto-deploy. Ручной vercel --prod только если auto-deploy сломан (free tier 100/сутки).
- DNS/почта: Cloudflare (DNS + Email Routing -> Brevo -> Gmail), vibecraft.kz.
- Блог: генерит vela-marketing-bot -> PR в borisk85/vibecraft, правка в /admin/blog/[pr], контент в lib/blog-posts.ts.
- Позиционирование: не «дешевле» — скорость + исход.
- Source of truth по ценам/услугам: components/sections/Services.tsx и Pricing.tsx.

## СПРАВКА (перенесено из memory 2026-07-18)

**Услуги и деньги:** 6 направлений (боты, AI-агенты, MVP веб, AI-сайты, автоматизации, MVP мобильных). Все цены «от», минимальный чек 150 000 ₸, точный расчет — кастомный PDF после ТЗ. Оплата 50/50 (предоплата / после демо), 14 дней бесплатных правок (баги и мелочи по ТЗ, не фичи), на ЕСП/ИП. Поддержка: «По часу» 25k/ч, «Базовый» 90k/мес (до 5ч), «Расширенный» 240k/мес (до 15ч); новые фичи = отдельный проект. Скидка повторным −10-15% (внутреннее, не афишировать). GEO и Speed Boost — услуги ВНУТРИ vibecraft, не отдельный бренд/домен, цены не подтверждены.

**Сроки (подтверждены, не выдумывать):** вся линейка макс 1-2 недели; бот до недели; MVP веб/мобилка до 2 недель.

**MVP-границы:** рабочий прототип одной фичи (авторизация + БД + деплой + минимальный UI), НЕ готовый SaaS. Не входит: админка/дашборды, тарифы/роли, локализация, CRM-интеграции, полная полировка. Мобильный — Flutter, сборка APK/TestFlight; без сторов, push, IAP.

**Инфра/лиды:** DNS только через Cloudflare (ps.kz — регистратор, не авторитетен). Client email — Resend (hello@vibecraft.kz, домен verified, вебхук событий в Telegram). Лиды форм и калькулятора падают в Telegram @vibecraft_leads_bot (CHAT_ID 661638470), email-дубль не нужен. Аналитика YM/GA ID — пусто.

**Не поломать (код):** services (Services.tsx) и faqs (FAQ.tsx) — именованные экспорты, single source для UI + JSON-LD. Логотип — lib/logo-path.ts (V_PATH). PDF-смета: логотип через Svg+Path (не Image), Inter по HTTP, кириллица .normalize("NFC") — иначе tofu.

**Отложено (docs/backlog.md — единый источник):** Cases → /cases при 3+ кейсах; GitHub org @vibecraft при первом платном клиенте.

**Маркетинг (docs/marketing-plan.md):** порядок каналов — партнерства с агентствами (аутрич через скрейпинг позже). Ads/IG/YouTube пока не предлагать.

## ПОМНИТЬ
- Шаблон писем клиентам lib/email-layout.ts зафиксирован Boris 08.09.2026: не трогать. Сейчас там квадрат лого 56px с иконкой 40px, слово vibecraft 18px, подпись 10px, иконки соцсетей серые #8a8a8a на прозрачном фоне (app/email-icon/[name]/route.ts). Тестовые письма собирать только через Bash — PowerShell портит кириллицу при копировании шаблона.
- Скриншоты в блоге снимаю сам через Playwright (реальные продукты, WebP, размеры в lib/blog-images.ts). Скрины, требующие входа Boris в кабинет, не запрашиваем.
